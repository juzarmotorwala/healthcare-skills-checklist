// Deprecated: replaced by SubmitButton.tsx (email-only submit flow).
// Left as an empty stub because this file couldn't be deleted from the mounted folder —
// safe to manually delete src/components/SavePdfButton.tsx, it's unused and not imported anywhere.
export {};
